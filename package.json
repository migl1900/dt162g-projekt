{
  "name": "workoutlog",
  "version": "1.0.0",
  "description": "Logging workout sessions",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd app && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.16",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "^12.16.1"
  },
  "author": "Michael Glimmerdahl",
  "license": "ISC"
}
